## TDSQL for MySQL 세부 정보 확인

- 대상 데이터베이스가 TDSQL for MySQL이고 마이그레이션할 데이터베이스에 'sysdb'라는 데이터베이스가 포함되어 있으면 마이그레이션되지 않습니다.
- 참고: `sysdb`는 TDSQL for MySQL의 시스템 데이터베이스입니다. 마이그레이션할 데이터베이스의 이름이 'sysdb'인 경우 마이그레이션 작업은 알람을 전송하지만 계속해서 정상적으로 실행됩니다.

## 문제 해결

마이그레이션할 데이터베이스 목록에서 `sysdb`를 제거합니다.

