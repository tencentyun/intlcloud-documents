After establishing the SSL VPN connection between Tencent Cloud and the client, you can use `ping` to test it.
For example, you can use the CVM in the VPC to `ping` an IP address in the mobile client IP range. If the pinging succeeds, the VPC and IDC can communicate with each other.
