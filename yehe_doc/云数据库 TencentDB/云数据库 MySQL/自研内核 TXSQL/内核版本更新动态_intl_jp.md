このドキュメントでは、MySQLカーネルバージョンに関する最新情報について説明します。アップグレードが必要な場合は、[カーネルのマイナーバージョンのアップグレード](https://intl.cloud.tencent.com/document/product/236/36816)をご参照ください。

## MySQL 8.0
### 20200630
#### 新機能：
- 大きなテーブルの非同期削除可能：ファイルを非同期かつ低速にクリーンアップすることで、大きなテーブルの削除によるサービスパフォーマンスのジッターを回避します。この機能を有効化するには、[作業依頼書をサブミット](https://console.cloud.tencent.com/workorder/category)して申請する必要があります。
- アイドルタスクを自動的にkillしてリソースの競合を減らすことをサポートします。この機能を有効化するには、[作業依頼書をサブミット](https://console.cloud.tencent.com/workorder/category)して申請する必要があります。
- 透過的データ暗号化（TDE）をサポートします。


#### 公式バグの修正：
- relay_log_posとmaster_log_posの間のチェックポイントに一貫性がないため、切り替えに失敗するエラーを修正します。
- 非同期で書き込むことにより発生するデータファイルエラーを修正します。
- fsyncがEIOを返すことを繰り返してデッドループに陥ってしまうという不具合を修正します。
- フルテキストインデックスにおけるマルチバイト文字セットでのフレーズ検索（phrase search）に発生したクラッシュ問題を修正します。

## MySQL 5.7
### 20200630
#### 新機能：
- SELECT FOR UPDATE/SHARE文におけるNOWAITとSKIP LOCKEDオプションの使用をサポートします。
- 大規模トランザクションの最適化機能をサポートし、大規模トランザクションに起因するマスター・スレーブの遅延、バックアップの失敗などの問題を軽減します。
- 監査パフォーマンスの最適化：非同期監査機能をサポートします。

#### 公式バグの修正：
- digest_add_token関数のオーバーフローを修正します。
- insert blobによるインスタンスのcrashを修正します。
- hash scanを実行すると、event内の同じ行が更新されたがレコードが見つからないために発生したマスター・スレーブの割り込みという不具合を修正します。
- performance_schemaをクエリーするときにhangするという不具合を修正します。

### 20200331
#### 新機能：
- 公式MySQL 5.7.22バージョンのJSONシリーズ関数が追加されました。
- 電子商取引のフラッシュセール（seckill）ケースに基づく[ホットスポットの更新](https://intl.cloud.tencent.com/document/product/1035/36037)機能をサポートしています。
- [SQLトラフィック制限](https://intl.cloud.tencent.com/document/product/1035/36037)をサポートしています。
- データ暗号化機能は、KMSカスタマイズキーの暗号化をサポートしています。

#### 公式バグの修正：
- フルテキストインデックスにおけるマルチバイト文字セットでのフレーズ検索（phrase search）に発生したクラッシュ問題を修正します。
- 同時実行数が多い場合、CATSロックスケジューリングモジュールに発生したクラッシュ問題を修正します。

### 20190830
#### 新機能：
- binlogファイルが破損した場合にスキップして解析を続ける機能をサポートします。マスターインスタンスとbinlogの両方が破損した場合に、最大限にスタンバイデータベースからデータを復元して利用します。
- 非GTIDモードからGTIDモードへのデータ同期をサポートします。
- ユーザーがshow full processlistステートメントを使用して「ユーザースレッドのメモリ使用状況」を確認することをサポートします。
- テーブルの[列の高速作成機能](https://intl.cloud.tencent.com/document/product/236/35990)をサポートし、データをコピーせず、ディスク容量やディスクI/Oを消費せず、サービスのピーク時にもリアルタイムで変更できます。
- 永続的な自動インクリメント値をサポートします。

#### 公式バグの修正：
- GRANTステートメントの列名に予約語がある場合にレプリケーションが中断するという不具合を修正します。
- パーティションテーブルで逆方向スキャンを実行することによってSQLの実行効率が低下するという不具合を修正します。
- プライマリキーを含むテーブルの仮想インデックスのデータ不整合によって、クエリ結果に異常が発生するという不具合を修正します。
- InnoDBでプライマリキーの範囲を検索することによって、データが欠落するという不具合を修正します。
- スペースインデックスを持つテーブルでDDLを実行することによるシステムのcrashを修正します。
- binlogのサイズが大きすぎると、ハートビート情報内のファイルの長さがオーバーフローし、マスター・スレーブが中断するという不具合を修正します。
- eventの削除によって他のeventが時間通りに実行されなくなるという不具合を修正します。
- 検索結果をまとめるとエラーが発生するという不具合を修正します。

### 20190615
#### 新機能：
- 透過的データ暗号化（TDE）をサポートします。

### 20190430
#### 公式バグの修正：
- サブクエリで長いテキスト機能を使用した場合のNULLポインタ参照の不具合を修正します。
- Hash Scanを実行するとマスター・スレーブが中断するという不具合を修正します。
- マスターデータベースのbinlogの切り替えによってスレーブノードのI/Oスレッドが中断するという不具合を修正します。
- NAME_CONSTによって引き起こされるクラッシュを修正します。
- 文字セットによるillegal mix of collationを修正します。


### 20190203
#### 新機能：
- 大きなテーブルの非同期削除可能：ファイルを非同期かつ低速にクリーンアップすることで、大きなテーブルの削除によるサービスパフォーマンスのジッターを回避します。この機能を有効化するには、[作業依頼書をサブミット](https://console.cloud.tencent.com/workorder/category)して申請する必要があります。
- CATSロックスケジューリング方式をサポートします。
- GTIDを有効化するとき、トランザクション内の一時テーブルおよびCTS構文を作成・削除することをサポートします。この機能を有効化するには、[作業依頼書をサブミット](https://console.cloud.tencent.com/workorder/category)して申請する必要があります。
- 暗黙的なプライマリキーをサポートします。この機能を有効化するには、[作業依頼書をサブミット](https://console.cloud.tencent.com/workorder/category)して申請する必要があります。
- super権限以外のユーザーが他のユーザーのセッションをkillする機能をサポートします。cdb_kill_user_extraパラメータを使用して設定します。デフォルト値はroot@%です。
- エンタープライズクラスの暗号化関数をサポートします。この機能を有効化するには、[作業依頼書をサブミット](https://console.cloud.tencent.com/workorder/category)して申請する必要があります。

#### 公式バグの修正：
- binlogキャッシュファイルの容量が不足している場合にレプリケーションが中断されるという不具合を修正します。
- fsyncがEIOを返すことを繰り返してデッドループに陥ってしまうという不具合を修正します。
- GTIDの欠番によってレプリケーションが中断され、再開できなくなるという不具合を修正します。
  

### 20180918
#### 新機能：
- アイドル状態のトランザクションをkillしてリソースの競合を減らすことをサポートします。この機能を有効化するには、[作業依頼書をサブミット](https://console.cloud.tencent.com/workorder/category)して申請する必要があります。
- MemoryエンジンからInnoDBエンジンへの自動切り替え：グローバル変数cdb_convert_memory_to_innodbがONの場合、テーブルエンジンはテーブルの作成/変更時にMemoryからInnoDBに変換されます。
- インデックスの非表示機能をサポートします。
- Jemallocによるメモリ管理をサポートします。これにより、jlibcメモリ管理モジュールを置き換え、メモリ使用量を削減し、メモリ割り当て効率を向上させることができます。
  
#### パフォーマンスの最適化：
- binlogの切り替えを最適化し、rotateのホールドロック時間を短縮させることで、システムパフォーマンスを向上させます。
- Crash Recovery時の回復速度を向上させます。
  
#### 公式バグの修正：
- マスター・スレーブの切り替えによるeventの無効化の不具合を修正します。
- REPLAY LOG RECORDによるCrashを修正します。
- Lose index scansによってクエリ結果にエラーが発生するという不具合を修正します。


### 20180530
#### 新機能：
- SQL監査機能をサポートします。
- テーブルレベルの並列レプリケーションをサポートします。この機能を有効化するには、[作業依頼書をサブミット](https://console.cloud.tencent.com/workorder/category)して申請する必要があります。
  
#### パフォーマンスの最適化：
- slaveインスタンスのロックを最適化し、slaveインスタンスの同期パフォーマンスを向上させます。   
- select...limitステートメントのプッシュダウンを最適化します。
  
#### 公式バグの修正：
- relay_log_posとmaster_log_posの間のチェックポイントに一貫性がないため、切り替えに失敗するエラーを修正します。
- Crash on UPDATE ON DUPLICATE KEYによるCrashを修正します。
- JSONカラムのインポートによる「Invalid escape character in string.」のエラーを修正します。
  
### 20171130
#### 新機能：
- information_schema.metadata_locksビューをサポートし、現在のインスタンスにおけるMDLの付与及び待機ステータスをクエリします。
- ALTER TABLE NO_WAIT | TIMEOUT構文をサポートし、DDL操作に待機タイムアウトを与えます。この機能を有効化するには、[作業依頼書をサブミット](https://console.cloud.tencent.com/workorder/category)して申請する必要があります。
- スレッドプール機能をサポートします。この機能を有効化するには、[作業依頼書をサブミット](https://console.cloud.tencent.com/workorder/category)して申請する必要があります。

#### 公式バグの修正：
- bytes_dataに基づいてinnodb_buffer_pool_pages_dataを計算し、このパラメータのオーバーフローを回避します。
- 非同期モードで速度制限プラグインが使用できないという不具合を修正します。

   
## MySQL 5.6
### 20200915
#### 新機能：
- [SQLトラフィック制限](https://intl.cloud.tencent.com/document/product/1035/36037)機能をサポートします。

#### パフォーマンスの最適化：   
- buffer pool初期化は最適化を加速します。

#### 公式バグの修正：
- マスター・スレーブrename tableがhangするという不具合を修正します。 
- event_schedulerがdisableに設定され、cdb_skip_event_schedulerがonからoffに変更された場合に発生するcrashを修正します。 
- tencentrootの最大リンク数がsrv_max_n_threadsにカウントされないことによって、sync_wait_arrayに関するアサーションに失敗するという不具合を修正します。 
- 他のクラウドサービスのMySQL 5.6とTencent MySQL 5.6のシステムライブラリ内の一部のテーブルの構造が異なるため、マスターとスレーブが並列レプリケーションをオンにするときに発生するcrashを修正します。 
- INSERT ON DUPLICATE KEY UPDATE THE WRONG ROWを修正します。 
- index_mappingにエラーが発生するという不具合を修正します。 
- mtr失敗のbugを修正します。 
- hash scanを実行すると、event内の同じ行が更新されたがこのレコードが見つからないことによって発生するマスター・スレーブの割り込みという不具合を修正します。 

### 20190930
#### 新機能：
- ユーザーがshow full processlistステートメントを使用して「ユーザースレッドのメモリ使用状況」を確認することをサポートします。  

#### 公式バグの修正：
- スレーブデータベースのreplication filterによるgtid欠番の不具合を修正します。
- Binlogのサイズが大きすぎると、ハートビート情報内のファイルの長さがオーバーフローし、マスター・スレーブが中断するという不具合を修正します。
- 文字セットによるillegal mix of collationを修正します。
- Hash Scanを実行するとマスター・スレーブが中断するという不具合を修正します。
- NAME_CONSTの使用によるcrashを修正します。
- マスターデータベースのbinlogの切り替えによってslaveノードのI/Oスレッドが中断するという不具合を修正します。
- innodb_log_checusumによってバックアップに互換性がないという不具合を修正します。

### 20190530
#### 公式バグの修正：
- RCモードでダーティデータが読み込まれるという不具合を修正します。
- 一時テーブルを削除するとスレーブインスタンスの再生に失敗するという不具合を修正します。
- 同時実行数が多い場合のデッドロックの不具合を修正します。
  

### 20190203
#### 新機能：
- 大きなテーブルの非同期削除可能：ファイルを非同期かつ低速にクリーンアップすることで、大きなテーブルの削除によるサービスパフォーマンスのジッターを回避します。この機能を有効化するには、[作業依頼書をサブミット](https://console.cloud.tencent.com/workorder/category)して申請する必要があります。
- super権限以外のユーザーが他のユーザーのセッションをkillする機能をサポートします。cdb_kill_user_extraパラメータを使用して設定します。デフォルト値はroot@%です。
- GTIDを有効化にするとき、トランザクション内の一時テーブルおよびCTS構文を作成・削除することをサポートします。この機能を有効化するには、[作業依頼書をサブミット](https://console.cloud.tencent.com/workorder/category)して申請する必要があります。
  
#### パフォーマンスの最適化：   
- パーティションテーブルのレプリケーションと再生を最適化することで、パーティションテーブルの再生速度を向上させます。
  
#### 公式バグの修正：
- 一時的な空き容量が不足しているため、マスター・スレーブ間でデータが一致しないという不具合を修正します。
- ホットスポットレコードを更新するとハングアップするという不具合を修正します。
- 並列レプリケーションでのSeconds_Behind_Master値に異常が発生するという不具合を修正します。

### 20180915
#### 新機能：
- MEMORYエンジンからInnoDBエンジンへの自動切り替え：グローバル変数cdb_convert_memory_to_innodbがONの場合、テーブルエンジンはテーブルの作成/変更時にMEMORYからInnoDBに変換されます。
- アイドル状態のトランザクションをkillしてリソースの競合を減らすことをサポートします。この機能を有効化するには、[作業依頼書をサブミット](https://console.cloud.tencent.com/workorder/category)して申請する必要があります。
  
#### 公式バグの修正：
- REPLAY LOG RECORDによるcrashを修正します。
- decimalの精度によるマスター・スレーブ間の時間データが一致しないという不具合を修正します。


### 20180130
#### 新機能：
- スレッドプール機能をサポートします。この機能を有効化するには、[作業依頼書をサブミット](https://console.cloud.tencent.com/workorder/category)して申請する必要があります。
- slaveノードはレプリケーションフィルタリングルールの動的な変更をサポートします。

#### パフォーマンスの最適化：
- ドロップテーブルによるパフォーマンスのジッター。
  
#### 公式バグの修正：
- パスワード文字列の認証によるデータベースのcrashを修正します。
  
### 20180122
#### 新機能：
- SQL監査機能をサポートします。

#### 公式バグの修正：
- 整数オーバーフローの不具合を修正します。
- フルテキストインデックスにおけるクエリエラーを修正します。
- レプリケーション時のslaveマシンのcrashを修正します。
	
### 20170830
#### 公式バグの修正：
- 非同期モードでbinlog速度制限が無効化になるという不具合を修正します。
- buffer_poolステータスに異常が発生するという不具合を修正します。
- SEQUENCEと暗黙的なプライマリキーの競合を修正します。
  
### 20170228
#### 公式バグの修正：
- drop tableにおける文字コードのバグを修正します。
- replicate-wild-do-tableがdbを正しくフィルタリングできず、またはtableに小数点などの特殊文字を含むという不具合を修正します。
- スレーブデータベースからrotateイベントが発生した後にSQLスレッドが早く終了するという不具合を修正します。
  
### 20161130
#### パフォーマンスの最適化：
- lock_logロックを分割し、lock logにかかる時間を短縮させ、並列処理のパフォーマンスを向上させます。
- マスターデータベースのACKスレッドが独立して動作し、応答時間を向上させます。
- ファントムリードを防ぐために、ACKを待機している間、ユーザースレッドはkill機能を禁止します。
- sync_binlog !=1の場合は、不要なlock_syncロックになることを修正します。

