## MySQL 5.7
### 20190830バージョン
#### 新特性：
- binlogファイルが破損した場合にスキップして解析を続ける機能をサポートします。マスターインスタンスとbinlogの両方が破損した場合に、最大限にスレーブデータベースからリカバリしたデータを利用します。
- 非GTIDモードからGTIDモードへのデータ同期をサポートします。
- show full processlistを使用して「ユーザースレッドのメモリー使用状況」を確認することをサポートします。
- テーブルの[カラム高速追加機能](https://intl.cloud.tencent.com/document/product/236/35990)をサポートします。データをコピーせず、ディスク容量やディスクI/Oを消費せず、ピーク期でもリアルタイムで変更することができます。
- AUTO_INCREMENTの永続化をサポートします。

#### 修正：
- Grantのカラム名に予約語がある場合にレプリケーションが中断するという不具合を修正しました。
- パーティションテーブルで逆方向スキャンを実行するとSQLの実行効率が低下するという不具合を修正しました。
- プライマリキーを含むテーブルの仮想インデックスのデータ不整合によって、クエリ結果の異常が発生するという不具合を修正しました。
- InnoDBでプライマリキーを範囲検索するとデータが欠落するという不具合を修正しました。
- 空間インデックスを持つテーブルでDDLを実行するによって引き起こされるcrashという不具合を修正しました。
- binlogのサイズが大きすぎると、ハートビート情報内のファイルの長さがオーバーフローし、マスター／スレーブが中断するという不具合を修正しました。
- eventを削除すると他のeventが時間通りに実行されなくなるという不具合を修正しました。
- 検索結果をまとめるとエラーが発生するという不具合を修正しました。

### 20190615バージョン
#### 新特性：
- 透過的なデータ暗号化をサポートします。

### 20190430バージョン
#### 修正：
- サブクエリで長いテキスト機能を使用した場合のNULLポインタ参照の不具合を修正しました。
- Hash Scanを実行するとマスター／スレーブが中断するという不具合を修正しました。
- マスターライブラリのbinlogの切り替えによってスレーブノードのI/Oスレッドが中断するという不具合を修正しました。
- NAME_CONSTの使用によるクラッシュの不具合を修正しました。
- 文字セットによるillegal mix of collationの不具合を修正しました。


### 20190203バージョン
#### 新特性：
- 大きなテーブルの非同期削除をサポートします：ファイルを非同期でゆっくりと削除することで、大きなテーブルの削除によるサービスパフォーマンスのジッタを回避します。この機能を有効化するには、[作業依頼書を提出](https://console.cloud.tencent.com/workorder/category)を使用しなければなりません。
- CATSのスケジューラをロックする方式をサポートします。
- GTIDを有効にすると、トランザクション内の一時テーブル及びCTS構文を作成、削除することができます。この機能を有効化するには、[作業依頼書を提出](https://console.cloud.tencent.com/workorder/category)を使用しなければなりません。
- 暗黙的なプライマリキーをサポートします。この機能を有効化するには、[作業依頼書を提出](https://console.cloud.tencent.com/workorder/category)を使用しなければなりません。
- super権限以外のユーザーが他のユーザーのセッションをkillすることをサポートします。cdb_kill_user_extraパラメータを使用して設定します。デフォルト値はroot@%です。
- エンタープライズクラスの暗号化関数をサポートします。この機能を有効化するには、[作業依頼書を提出](https://console.cloud.tencent.com/workorder/category)を使用しなければなりません。

#### 修正：
- binlogキャッシュファイルの容量が不足している場合にレプリケーションが中断するという不具合を修正しました。
- fsyncがEIOを返すことを繰り返してデッドループに陥ってしまうという不具合を修正しました。
- GTIDの欠番によってレプリケーションが中断され、再開できないという不具合を修正しました。
   

### 20180918バージョン
#### 新特性：
- アイドルタスクを自動的にkillしてリソースの競合を減らすことをサポートします。この機能を有効化するには、[作業依頼書を提出](https://console.cloud.tencent.com/workorder/category)を使用しなければなりません。
- Memoryエンジンは自動的にInnoDBエンジンに変換されます：グローバル変数cdb_convert_memory_to_innodbがONの場合、テーブルエンジンはテーブルの作成/変更時にMemoryからInnoDBに変換されます。
- インデックスの非表示機能をサポートします。
- Jemallocメモリー管理をサポートします。jlibcメモリー管理モジュールを置き換え、メモリー使用量を削減し、メモリー割り当て効率を向上させます。
   
#### パフォーマンスの最適化：
- binlogの切り替えを最適化し、rotateのホールドロック時間を短縮させることで、システムパフォーマンスを向上させます。
- Crash Recovery時のリカバリ速度を向上させます。
    
#### 修正：
-  マスター・スレーブの切替によるeventの無効化の不具合を修正しました。
- REPLAY LOG RECORDによるCrashの不具合を修正しました。
- Lose index scansによってクエリ結果にエラーが発生するという不具合を修正しました。


### 20180530バージョン
#### 新特性：
- SQL監査機能をサポートします。
- テーブルレベルの並列レプリケーションをサポートします。この機能を有効化するには、[作業依頼書を提出](https://console.cloud.tencent.com/workorder/category)を使用しなければなりません。
   
#### パフォーマンスの最適化：
- slaveインスタンスのロックを最適化し、slaveインスタンスの同期パフォーマンスを向上させます。   
- select...limitのプッシュダウンを最適化します。
   
#### 修正：
- relay_log_pos&master_log_posのポジションの不一致による切り替えの失敗の不具合を修正しました。
- Crash on UPDATE ON DUPLICATE KEYによるCrashの不具合を修正しました。
- JSONカラムのインポートによる「Invalid escape character in string.」のエラーを修正しました。
   
### 20171130バージョン
#### 新特性：
- information_schema.metadata_locksビューをサポートし、現在のインスタンスにおけるMDLの付与及び待機ステータスをクエリします。
- ALTER TABLE NO_WAIT TIMEOUTの構文をサポートし、DDL操作に待機タイムアウトを与えます。この機能を有効化するには、[作業依頼書を提出](https://console.cloud.tencent.com/workorder/category)を使用しなければなりません。
- スレッドプール機能をサポートします。この機能を有効化するには、[作業依頼書を提出](https://console.cloud.tencent.com/workorder/category)を使用しなければなりません。

#### 修正：
- bytes_dataに基づいてinnodb_buffer_pool_pages_dataを計算し、このパラメータのオーバーフローを回避します。
- 非同期モードで速度制限プラグインが使用できないという不具合を修正しました。

   
## MySQL 5.6
### 20190930バージョン
#### 新特性：
- ユーザーは、how full processlistを使用して「ユーザースレッドのメモリー使用状況」をクリエすることができます。この機能を有効化するには、[作業依頼書を提出](https://console.cloud.tencent.com/workorder/category)を使用しなければなりません。  

#### 修正：
- スレーブデータベースのreplication filterによるgtid欠番の不具合を修正しました。
- Binlogのサイズが大きすぎると、ハートビート情報内のファイルの長さがオーバーフローし、マスター／スレーブが中断するという不具合を修正しました。
- 文字セットによるillegal mix of collationの不具合を修正しました。
- Hash Scanを実行するとマスター／スレーブが中断するという不具合を修正しました。
- NAME_CONSTの使い方によるcrashの不具合を修正しました。
- マスターライブラリのbinlogの切り替えによってスレーブノードのI/Oスレッドが中断するという不具合を修正しました。
- innodb_log_checusumによるバックアップに互換性がないという不具合を修正しました。

### 20190530バージョン
#### 修正：
- RCモードでダーティなデータが読み込まれるという不具合を修正しました。
- 一時テーブルを削除するとスレーブの再生が失敗するという不具合を修正しました。
- 高並列処理でのデッドロックの不具合を修正しました。
   

### 20190203バージョン
#### 新特性：
- 大きなテーブルの非同期削除：ファイルを非同期でゆっくりと削除することで、大きなテーブルの削除によるサービスパフォーマンスのジッタを回避します。この機能を有効化するには、[作業依頼書を提出](https://console.cloud.tencent.com/workorder/category)を使用しなければなりません。
- super権限以外のユーザーが他のユーザーのセッションをkillすることをサポートします。cdb_kill_user_extraパラメータを使用して設定します。デフォルト値はroot@%です。
- GTIDを有効にすると、トランザクション内の一時テーブル及びCTS構文を作成、削除することができます。この機能を有効化するには、[作業依頼書を提出](https://console.cloud.tencent.com/workorder/category)を使用しなければなりません。
   
#### パフォーマンスの最適化：   
- パーティションテーブルの複製再生を最適化することで、パーティションテーブルの再生速度を向上させます。
   
#### 修正：
- 一時的な空き容量が不足しているために、マスター／スレーブが一致しないという不具合を修正しました。 
- ホットログレコードを更新するとハングアップするという不具合を修正しました。
- 並列レプリケーションでのSeconds_Behind_Master値に異常が発生するという不具合を修正しました。

### 20180915バージョン
#### 新特性：
- MEMORYエンジンは自動的にInnoDBエンジンに変換されます：グローバル変数cdb_convert_memory_to_innodbがONの場合、テーブルエンジンはテーブルの作成/変更時にMEMORYからInnoDBに変換されます。
- アイドルタスクを自動的にkillし、リソースの競合を減らします。この機能を有効化するには、[作業依頼書を提出](https://console.cloud.tencent.com/workorder/category)を使用しなければなりません。
   
#### 修正：
- REPLAY LOG RECORDによるcrashの不具合を修正しました。
- decimalの精度によるマスター／スレーブの時間データの不一致の不具合を修正しました。


### 20180130バージョン
#### 新特性：
- スレッドプール機能をサポートします。この機能を有効化するには、[作業依頼書を提出](https://console.cloud.tencent.com/workorder/category)を使用しなければなりません。
- slaveノードはレプリケーションフィルタリング条件の動的な変更をサポートします。

#### パフォーマンスの最適化：
- drop tableによるパフォーマンスのジッタ。
   
#### 修正：
- 認証パスワード文字列によってデータベースのcrashを引き起こすという不具合を修正しました。
   
### 20180122バージョン
#### 新特性：
- SQL監査機能をサポートします。

#### 修正：
- 整数がオーバーフローするという不具合を修正します。
- 全文索引を使用したクエリにエラーが発生するという不具合を修正しました。
- レプリケーション時にslaveがcrashするという不具合を修正しました。
	
### 20170830バージョン
#### 修正：
- 非同期モードでbinlog速度制限が無効化になるという不具合を修正しました。
- buffer_poolステータスに異常が発生するという不具合を修正しました。
- SEQUENCEと暗黙的なプライマリキーの競合を修正しました。
   
### 20170228バージョン
#### 修正：
- drop tableの文字コードのbugを修正しました。
- replicate- wild- do- tableが小数点などの特殊文字を含むdb又はtableを正しくフィルタできないという不具合を修正しました。
- スレーブデータベースからrotateイベントが発生した後にSQLスレッドが早期に終了するという不具合を修正しました。
  
### 20161130バージョン
#### パフォーマンスの最適化：
- lock_logロックを分割し、lock logにかかる時間を短縮させ、並列処理のパフォーマンスを向上させます。
- マスターデータベースのACKスレッドが独立に動作するようにして、応答時間が向上します。
- ユーザスレッドはファントムリードを防ぐためにACK待ちの間にkill機能が許可されません。
- sync_binlog !=1の場合は、不要なlock_syncロックになることを修正しました。

