**规则含义：**检查 CAM 访问管理是否开启了登陆保护MFA。
**合规检测逻辑：**CAM 访问管理开启了登陆保护MFA，则符合规则。
**规则标志符：**`cam-account-login-mfa-enabled`
**风险等级：**高
**适用的资源类型：**`QCS::CAM::User`
**规则触发类型：**周期执行，24小时
**关键词：**用户，MFA
**规则入参：**无
