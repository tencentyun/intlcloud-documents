You can delete a VPC if you no longer need it. Before deleting the VPC, please make sure that its IP addresses are not used, and there’re no cloud resources on it, including CVMs, TencentDB, gateways and others.

## Directions
1. Log in to the [VPC console](https://console.cloud.tencent.com/vpc).
2. Select the region of the VPC at the top of the **VPC** page.
3. In the VPC list, locate the VPC to delete, click **Delete** under the **Operation** column, and confirm the deletion.

