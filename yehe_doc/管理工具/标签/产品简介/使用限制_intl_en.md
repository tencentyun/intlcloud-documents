
## Quantity

- A tag consists of 1 tag key and 1 tag value (tagKey:tagValue).
- Each resource can have 50 tags at most.
- For each resource, each tag key can have only 1 value.




## Naming

<table>
<tr>
<th >Tag Key</th>
<th >Tag Value</th>
</tr><tr>
<td>Tag keys starting with `qcs:` or `project:` are reserved for Tencent Cloud use. Do not use the prefixes when you create a tag key.</td>
<td>-</td>
</tr>
<tr>
<td>In UTF-8, a tag key must be 1 to 127 Unicode characters.</td>
<td>In UTF-8, a tag value must be 1 to 255 Unicode characters.</td>
</tr>
<tr>
<td colspan=2>Supports UTF-8-encoded characters, digits, spaces, and special characters (+-=._:/@()[]（）【】)</td>
</tr>
<tr>
<td colspan=2>Tag keys and values are case sensitive.</td>
</tr>
</tbody></table>
