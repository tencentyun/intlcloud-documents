### Server Fleet
Server fleet, aka fleet, is a group of CVM instances capable of auto scaling and health check.

### Alias
An alias generally points to one server fleet. Therefore, you can use multiple aliases to point to different fleets.

### Code Resource Package
Code resource package, aka asset, refers to a program package deployed on a CVM instance.
